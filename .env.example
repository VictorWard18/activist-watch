# activist-watch secrets — copy to .env and fill in. NEVER commit .env.
# Telegram bot: create with @BotFather, chat_id from @userinfobot
AW_TELEGRAM_BOT_TOKEN=
AW_TELEGRAM_CHAT_ID=

# Phase 3 (LLM enrichment) — optional
ANTHROPIC_API_KEY=

# Phase 2 (X monitoring via Grok) — optional
XAI_API_KEY=
